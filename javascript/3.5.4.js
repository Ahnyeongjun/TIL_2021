const colorsArray = ['a', 'b', 'c'];
console.log(colorsArray[0]);

const colorObj = {
    '0': 'a',
    '1': 'b',
    '2': 'c'
};

console.log(colorObj[0]);
console.log(colorObj.length);
console.log(colorsArray.length);