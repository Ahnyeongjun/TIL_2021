const intNum = 10;
const floatNum = 0.3;

const singleQuoteStr = 'single string';
const doubleQuoteStr = 'double string';
const singleChar = 'a';

const boolVar = true;

let emptyVar;
const nullVar = null;
console.log(
    typeof intNum,
    typeof floatNum,
    typeof singleQuoteStr,
    typeof doubleQuoteStr,
    typeof singleChar,
    typeof boolVar,
    typeof nullVar,
    typeof emptyVar,
)